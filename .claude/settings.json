{
  "enabledPlugins": {},
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty'); [ -z \"$FILE\" ] && exit 0; case \"$FILE\" in *.py) ruff check \"$FILE\" 2>&1 | head -20 ;; *.rs) rustfmt --check \"$FILE\" 2>&1 | head -20 ;; esac",
            "timeout": 10,
            "statusMessage": "Linting..."
          }
        ]
      }
    ]
  }
}
